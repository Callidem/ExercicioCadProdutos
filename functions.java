//from prettytable import PrettyTable
/*
public class functions{
  public double Produtos(){
    
    
    
    while (qteAtual > qteMax or qteAtual < 0):
      qteAtual = int(input("A quantidade atual não pode ser maior que a quantidade máxima, nem menor que 0. Insira a quantidade em estoque novamente: "))
    
    dicAux = dict(Rotulo = rotulo, QuantidadeMinima = qteMin, QuantidadeAtual = qteAtual, QuantidadeMaxima = qteMax, ValorUnitario =  valorUnit)
    Produtos.append(dicAux)
  
    return qteAtual * valorUnit;
  }
  
  public class printTabelaReposicao(Produtos){
    tabelaMercReposicao = PrettyTable()
    tabelaMercReposicao.field_names = ["Produto", "Quantidade em estoque", "Quantidade mínima", "Quantidade máxima"]
    for i in range(0, len(Produtos), 1):
      if(Produtos[i]["QuantidadeMinima"] > Produtos[i]["QuantidadeAtual"] ):
        tabelaMercReposicao.add_row([Produtos[i]["Rotulo"], str(Produtos[i]["QuantidadeAtual"]), str(Produtos[i]["QuantidadeMinima"]), str(Produtos[i]["QuantidadeMaxima"]) ])
  
    print(tabelaMercReposicao)
  }
  
  public printTabelaProdutos(Produtos):
    tabelaProdutos = PrettyTable()
    tabelaProdutos.field_names = ["Produto", "Valor Unitário", "Quantidade em estoque", "Quantidade mínima", "Quantidade máxima"]
    for i in range(0, len(Produtos), 1):
      #if(Produtos[i]["QuantidadeMinima"] > Produtos[i]["QuantidadeAtual"] ):
      tabelaProdutos.add_row([ Produtos[i]["Rotulo"], str(Produtos[i]["ValorUnitario"]), str(Produtos[i]["QuantidadeAtual"]), str(Produtos[i]["QuantidadeMinima"]), str(Produtos[i]["QuantidadeMaxima"]) ])
  
    print(tabelaProdutos)
  }    
}*/
    